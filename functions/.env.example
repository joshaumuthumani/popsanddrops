# Cloud Functions config for Phase 2 results email (server-side only).
# Copy to functions/.env and fill in. functions/.env is gitignored.

RESEND_API_KEY=
RESEND_FROM="Pops & Drops <noreply@yourdomain.com>"
APP_PUBLIC_URL=https://popsanddrops.web.app
